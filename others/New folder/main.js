var firstnum,secondnum,num1,num2,sum;

firstnum=window.prompt("Enter first num",0);
secondnum=window.prompt("Enter first num",0);
num1=parseInt(firstnum);
num2=parseInt(firstnum);
sum=num1+num2;
document.write(num1+" + "+num2+" is "+sum);